[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
source = ["src"]
omit = ["src/chat.py", "src/evaluate.py"]

[tool.coverage.report]
show_missing = true
fail_under = 0
